<h3><code>$ rake -T</code></h3>

<table>
  <% @tasks.each do |task, desc| %>
    <tr>
      <td><a href="<%= "#{@path}/#{task}/" %>"><%= task %></a></td>
      <td><%= desc %></td>
    </tr>
  <% end %>
</table>
